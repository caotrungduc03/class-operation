"use client";
import { Typography } from "antd";

const HomeLMS: React.FC = () => {
  return (
    <main>
      <div className="flex h-screen w-screen justify-center">
        <Typography.Title level={1}>Welcome to LMS</Typography.Title>
      </div>
    </main>
  );
};

export default HomeLMS;
