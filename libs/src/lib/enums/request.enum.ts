export enum RequestType {
  WEEKLY_NORM = 'WEEKLY_NORM',
}

export enum RequestStatus {
  PENDING = 'PENDING',
  APPROVED = 'APPROVED',
  REJECTED = 'REJECTED',
  CANCELED = 'CANCELED',
}

export enum RequestAction {
  APPROVE = 'APPROVE',
  CANCEL = 'CANCEL',
}
